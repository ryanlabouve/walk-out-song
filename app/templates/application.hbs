<div class=" max-w-lg m-auto text-center mt-16">
  <h1 class="text-6xl text-gray-600">Walkout song</h1>
  <div class="flex my-8">
    <div class="p-2 text-6xl flex-grow">
      😍
    </div>
    <div class="p-2 text-6xl flex-grow">
      💩
    </div>
    <div class="p-2 text-6xl flex-grow">
      😭
    </div>
    <div class="p-2 text-6xl flex-grow">
      🤬
    </div>
  </div>
  <div>
    <input
      onkeyup={{perform searchResultsTask value="target.value"}}
      placeholder="choose song"
      class="mb-4 rounded-full shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline"
    >
  </div>
  {{#if searchResultsTask.isRunning}}
    <LoadingSpinner />
  {{else}}
    {{#if searchTerm}}
      {{#each searchResults as |searchResult|}}
        Match found!
      {{else}}
        No matches found
      {{/each}}
    {{/if}}
  {{/if}}
</div>